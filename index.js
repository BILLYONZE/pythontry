/*Here is where i start my JS code.
 I will be using the express framework to create a simple web server that serves a static HTML file.
 I will also use the fs module to read the HTML file from the filesystem.*/
 console.log("Starting the server...");